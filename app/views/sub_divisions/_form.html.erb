<%= form_with(model: [division,sub_division],url: disco_region_division_sub_divisions_path(disco,region,division)) do |form| %>
  <% if sub_division.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sub_division.errors.count, "error") %> prohibited this sub_division from being saved:</h2>

      <ul>
        <% sub_division.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :division_id, style: "display: block" %>
    <%= form.text_field :division_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
