<p style="color: green"><%= notice %></p>

<h1>Regions</h1>

<div id="regions">
  <% @regions.each do |region| %>
    <%= render region %>
    <p>
      <%= link_to "Show this region", disco_region_path(region.disco_id,region) %>
    </p>
  <% end %>
</div>

<%= link_to "New region", new_disco_region_path %>
